SELECT names FROM (SELECT DISTINCT people.id, people.name AS names FROM ((people JOIN directors ON people.id = directors.person_id) JOIN movies ON directors.movie_id = movies.id JOIN ratings ON movies.id = ratings.movie_id) WHERE ratings.rating >= "9.0")